{% extends 'base.html.twig' %}

{% block title %}Modifier l'adresse{% endblock %}

{% block body %}
    <h1>Modifier l'adresse</h1>

    {{ include('adresse/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="btn-block">
        <a class="btn btn-primary" href="{{ path('adresse_index', {'contact_id': adresse.contact.id}) }}">Retour</a>
        {{ include('adresse/_delete_form.html.twig') }}
    </div>
{% endblock %}
